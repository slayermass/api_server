---contentone---
remove: убрать status_content, fk_user_created

---mysql/content 168---
метод для получения авторов + модель ИЛИ
    left join при запросе контента + в базе внешний ключ
data.author = await mysql

---contentone---
withimages public(url_file, ext) + private(все)
